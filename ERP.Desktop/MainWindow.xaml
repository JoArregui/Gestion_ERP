<Window x:Class="ERP.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="ERP .NET — Escritorio" Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#020617">
    <Grid>
        <!-- WebView2 host: navega al API que sirve el Blazor (tal cual web) -->
        <wv2:WebView2 x:Name="MainWebView" />

        <!-- Overlay carga -->
        <Grid x:Name="LoadingOverlay" Background="#020617" Visibility="Visible">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border Width="64" Height="64" Background="#2563eb" CornerRadius="16" >
                    <TextBlock Text="E" Foreground="White" FontSize="28" FontWeight="Black" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>
                <TextBlock Text="ERP .NET — Escritorio" Foreground="White" FontSize="16" FontWeight="Black" Margin="0,16,0,4" HorizontalAlignment="Center"/>
                <TextBlock x:Name="LoadingText" Text="Iniciando motor local..." Foreground="#64748b" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                <ProgressBar IsIndeterminate="True" Width="200" Height="3" Margin="0,16,0,0" Foreground="#2563eb" Background="#1e293b" BorderThickness="0"/>
            </StackPanel>
        </Grid>

        <!-- Barra inferior estado -->
        <Border VerticalAlignment="Bottom" Background="#0f172a" BorderBrush="#1e293b" BorderThickness="0,1,0,0" Padding="8,4">
            <DockPanel>
                <TextBlock x:Name="StatusText" Text="Desconectado" Foreground="#64748b" FontSize="10" VerticalAlignment="Center" DockPanel.Dock="Left"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Content="⟳ Recargar" Click="Reload_Click" Padding="8,2" Margin="4,0" FontSize="10" Background="#1e293b" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="🛠 DevTools" Click="DevTools_Click" Padding="8,2" Margin="4,0" FontSize="10" Background="#1e293b" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
